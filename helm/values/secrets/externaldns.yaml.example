# External-DNS Secrets Template
# Copy this file to externaldns.yaml and update with actual password
# This file is committed to git as a reference

# NOTE: Helm REPLACES arrays instead of merging them!
# All extraArgs must be in this file (both server and password)
extraArgs:
  - --pihole-server=http://pihole-web.pihole-system.svc.cluster.local
  - --pihole-password=CHANGEME
